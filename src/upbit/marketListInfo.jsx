// import React, { useState } from "react";

// const MarketListInfo = ({ marketList }) => {
//   const [info, setInfo] = useState(
//     Array(marketList.includes("KRW-").length).fill(false)
//   );
//   console.log(info);

//   function handleClick(idx) {
//     const newInfo = Array(marketList.includes("KRW-").length).fill(false);
//     newInfo[idx] = true;
//     setInfo(newInfo);
//   }

//   return {marketList.map};
// };

// export default MarketListInfo;
