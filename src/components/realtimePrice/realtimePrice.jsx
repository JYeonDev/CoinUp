import { money } from "../../upbit/data";
import styles from "./realtimePrice.module.css";

const RealtimePrice = () => {
  return (
    <div>
      실시간가격
      <button>클릭</button>
    </div>
  );
};

export default RealtimePrice;
